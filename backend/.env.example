DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=linklearn
DB_USER=root
DB_PASS=

APP_ORIGIN=http://localhost:5173
SESSION_NAME=linklearn_session

UPLOAD_BASE_URL=/uploads

# Optional production push notifications through OneSignal.
# Create a Web Push app in OneSignal, use the same domain as APP_ORIGIN,
# then set both values below on the deployed backend.
ONESIGNAL_APP_ID=
ONESIGNAL_REST_API_KEY=
PUSH_DEFAULT_TITLE=Link & Learn
